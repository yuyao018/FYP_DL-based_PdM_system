# Environment Variables
.env

# Python
__pycache__/
*.pyc

# Dash cache
*.pickle

# Engine sensor data files — ignore org subfolders and their JSON files
# but keep the data/ folder itself tracked via .gitkeep
data/*/
!data/.gitkeep
